# Questions For Filipa Or Renato

- Are the original DSS seeds for October predModels available?
- Are original `ssdir.par` files for October runs available?
- Should `STD` be interpreted as variance, as in `readoutput_AUV.m`, or should it be square-rooted later?
- Is exact reproduction required, or are distributional/statistical matches acceptable?
- Are `scene.gslib` coordinate side-products required for downstream use, or only NetCDF `TEMPpred`/`STD`?
